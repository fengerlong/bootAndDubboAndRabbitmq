<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
       xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:context="http://www.springframework.org/schema/context"
       xmlns:dubbo="http://code.alibabatech.com/schema/dubbo"
       xsi:schemaLocation="http://www.springframework.org/schema/beans
        http://www.springframework.org/schema/beans/spring-beans.xsd
        http://www.springframework.org/schema/context
        http://www.springframework.org/schema/context/spring-context-3.0.xsd
        http://code.alibabatech.com/schema/dubbo
        http://code.alibabatech.com/schema/dubbo/dubbo.xsd">

    <!--应用的名称,以及维护人员姓名-->
    <dubbo:application name="${dubbo.application.name}" owner="fel" />

    <!--使用什么协议作为注册中心,以及注册中心的地址-->
    <dubbo:registry protocol="${dubbo.registry.protocol}" address="${dubbo.registry.address}" />

    <!--使用什么通信协议,以及端口,dubbo支持多协议如dubbo hessian webService等-->
    <dubbo:protocol name="${dubbo.protocol.name}" port="${dubbo.protocol.port}" />
    
    <!--<dubbo:service interface="com.dubbo.test.client.service.TestService"-->
                   <!--ref="testService"-->
                   <!--timeout="40000"-->
                   <!--version="1.0.0"-->
                   <!--protocol="dubbo">-->
    <!--</dubbo:service>-->

    <dubbo:reference id="testService"
                     interface="com.dubbo.test.dubboclient.service.TestService"
                     protocol="dubbo"
                     check="false"
                     version="1.0.0">
    </dubbo:reference>


</beans>